#include <bits/stdc++.h>
using namespace std;

int main() {
    int a, b;
    string s;

    // 標準入力から、空白や改行で区切られた整数と文字列を読み込む。
    cin >> a >> b >> c >> s;

    // 整数と文字列を空白で区切って、標準出力に書き出す。
    cout << a + b + c<< " " << s << endl;
    return 0;
}